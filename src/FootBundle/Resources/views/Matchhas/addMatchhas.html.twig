{% extends "::base2.html.twig" %}
{% block stylesheets %}
    <link href="{{ asset('src/css/summernote/summernote.css') }}" rel="stylesheet">
    <link href="{{ asset('src/css/summernote/summernote-bs3.css') }}" rel="stylesheet">
    <link href="{{ asset('src/css/datapicker/datepicker3.css') }}" rel="stylesheet">

{% endblock %}

 {% block actionArea %}
     <a href="{{ path('foot_list_Matchhas') }}" class="btn btn-primary"><i class="fa fa-list-ul"></i> Match list</a>
 {% endblock %}
{% block titleContaint %}
    <h2>Add Match</h2>
{% endblock %}

{% block breadcrumb %}
    <ol class="breadcrumb">
        <li>
            <a>Match</a>
        </li>
        <li class="active">
            <strong>Add Match</strong>
        </li>
    </ol>
{% endblock %}
{% block MatchhasActive %}class="active"{% endblock %}
{% block MatchhasAddctive %}class="active"{% endblock %}
{% block Containt %}
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h5>Video</h5>
                </div>
                <div class="ibox-content">

                    <form method="post" action="{{ path('foot_add_Matchhas') }}" enctype="multipart/form-data">
                        {{ form_row(form._token) }}
                        {% if not form.vars.valid %}
                            <div class="alert alert-danger">
                                {{ form_errors(form) }}
                            </div>
                        {% endif %}
                        {% if app.session.flashBag.has('success') %}
                            {% for flashMessage in app.session.flashbag.get('success') %}
                                <div class="alert alert-success">
                                    {{ flashMessage }}
                                </div>
                            {% endfor %}
                        {% endif %}

                        <div class="form-group">
                            <label>Equipe 1</label>
                            {{ form_widget(form.equipe_1) }}
                        </div>
                        <div class="form-group">
                            <label>Equipe 2</label>
                            {{ form_widget(form.equipe_2) }}
                        </div>

                        <div class="form-group">
                            <label for="repeatPassword">date </label>
                            {{ form_widget(form.equipe_date) }}
                        </div>

                        <div class="form-group">
                            <label for="repeatPassword">Public </label>
                            {{ form_widget(form.match_has_active) }}
                        </div>

                        <div class="form-group">
                            <button type="submit" class="btn btn-primary pull-right"><i class="fa fa-plus-square-o"></i> Add</button>
                        </div>
                        <br>
                        <br>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('src/js/summernote/summernote.min.js') }}"></script>
    <script src="{{ asset('src/js/datapicker/bootstrap-datepicker.js') }}"></script>

    <script>
        $(document).ready(function(){

            $('.summernote').summernote();

        });
        $('#data_1 .input-group.date').datepicker({
            todayBtn: "linked",
            keyboardNavigation: false,
            forceParse: false,
            calendarWeeks: true,
            autoclose: true
        });
    </script>

{% endblock %}

